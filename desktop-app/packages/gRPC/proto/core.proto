syntax = "proto3";
package labcode.core;
// 占位 proto，后续按 TrieCode 的 board/compile/debug/monitor 拆分
service CoreService {
  rpc Ping(PingRequest) returns (PingResponse);
}
message PingRequest { string message = 1; }
message PingResponse { string reply = 1; }
